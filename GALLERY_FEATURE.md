# 图片展示功能说明

## 功能概述

新增的图片展示功能允许用户在主界面上查看批量文件夹和单个文件夹的翻译后图片。支持：

- **批量文件夹查看**：展示所有 `batch_` 开头的批量翻译结果
- **单个文件夹查看**：展示所有 `session_` 开头的单次翻译结果
- **图片单列展示**：每张图片占据一行，方便阅读漫画
- **大图预览**：点击图片查看大图，支持键盘导航
- **批量下载**：支持下载单张图片或整个文件夹（ZIP格式）

## 功能特点

### 1. 文件夹组织
- 左侧边栏显示所有批量文件夹和单个文件夹
- 每个文件夹显示包含的会话数或图片数
- 点击文件夹即可查看内容

### 2. 图片展示
- **单列布局**：每行显示一张完整图片，适合漫画阅读
- **响应式设计**：自动适应屏幕大小
- **懒加载**：优化性能，按需加载图片
- **悬浮效果**：鼠标悬停显示查看图标

### 3. 交互功能
- **大图预览**：
  - 点击图片打开全屏预览
  - 支持键盘导航（← → 键切换，ESC 关闭）
  - 显示图片序号和文件名
  
- **下载功能**：
  - 单张下载：点击每张图片下的下载按钮
  - 批量下载：点击"下载全部"按钮，打包为ZIP文件
  - 模态框下载：预览时可直接下载当前图片

## API接口

### 1. 获取批量文件夹列表
```
GET /api/gallery/batch-folders
```
返回所有batch_开头的文件夹及其包含的会话数量。

### 2. 获取单个文件夹列表
```
GET /api/gallery/session-folders
```
返回所有session_开头的文件夹及其包含的图片数量。

### 3. 获取批量文件夹内的会话
```
GET /api/gallery/batch-folders/{batchFolderName}/sessions
```
返回指定批量文件夹内的所有会话。

### 4. 获取文件夹内的图片列表
```
GET /api/gallery/images?folderPath={folderPath}
```
返回指定文件夹translated子目录下的所有图片信息。

### 5. 查看图片（内联显示）
```
GET /api/gallery/view?folderPath={folderPath}&filename={filename}
```
以内联方式返回图片文件，用于页面显示。

### 6. 下载单张图片
```
GET /api/gallery/download?folderPath={folderPath}&filename={filename}
```
下载指定图片文件。

### 7. 下载整个文件夹（ZIP）
```
GET /api/gallery/download-folder?folderPath={folderPath}
```
将文件夹内所有图片打包为ZIP文件下载。

## 使用方法

### 访问图片展示页面

1. **从主页进入**：
   - 打开主页 `http://localhost:8080/index.html`
   - 点击右上角的"查看翻译结果"按钮
   - 或直接访问 `http://localhost:8080/gallery.html`

2. **选择文件夹**：
   - 在左侧边栏中选择要查看的批量文件夹或单个文件夹
   - 批量文件夹会先显示包含的会话列表
   - 单个文件夹直接显示图片列表

3. **查看图片**：
   - 图片按文件名排序，单列显示
   - 滚动页面查看所有图片
   - 点击图片打开全屏预览

4. **下载图片**：
   - 单张下载：点击图片下方的"下载"按钮
   - 批量下载：点击页面顶部的"下载全部"按钮
   - 预览下载：全屏预览时点击"下载"按钮

## 文件结构

新增的文件：

```
src/main/
├── java/com/example/mangaTrans/
│   └── controller/
│       └── GalleryController.java     # 图片展示API控制器
└── resources/static/
    ├── gallery.html                    # 图片展示HTML页面
    ├── css/
    │   └── gallery.css                 # 图片展示样式
    └── js/
        └── gallery.js                  # 图片展示逻辑
```

修改的文件：
```
src/main/resources/static/
├── index.html                          # 添加了"查看翻译结果"入口按钮
└── css/
    └── style.css                       # 添加了按钮样式
```

## 技术实现

### 后端（GalleryController）
- 使用 Spring Boot RestController
- 扫描 storage 目录下的批量和单个文件夹
- 提供图片文件的内联显示和下载
- 支持ZIP文件打包下载

### 前端
- **HTML**：响应式布局，左右分栏设计
- **CSS**：现代化UI，支持深色模式友好
- **JavaScript**：
  - 动态加载文件夹和图片列表
  - AJAX异步请求API
  - 图片懒加载优化性能
  - 键盘导航支持

## 性能优化

1. **懒加载**：使用 `loading="lazy"` 属性，图片按需加载
2. **缓存**：浏览器自动缓存已加载的图片
3. **异步加载**：文件夹列表和图片列表异步加载，不阻塞页面渲染
4. **响应式**：根据屏幕大小自动调整布局

## 未来改进

- [ ] 添加图片排序选项（按名称、大小、时间）
- [ ] 支持图片缩略图网格视图
- [ ] 添加搜索和筛选功能
- [ ] 支持图片编辑和批注
- [ ] 添加收藏夹功能
- [ ] 支持对比查看原图和翻译图

## 故障排除

### 问题：图片无法显示
**解决方案**：
- 检查文件夹路径是否正确
- 确认 translated 子目录存在且包含图片
- 检查图片文件权限

### 问题：下载失败
**解决方案**：
- 检查浏览器是否允许下载
- 确认磁盘空间充足
- 检查服务器日志错误信息

### 问题：批量文件夹为空
**解决方案**：
- 确认已经完成批量翻译任务
- 检查 storage 目录下是否有 batch_ 开头的文件夹
- 刷新页面重新加载

## 许可证

本功能遵循项目主许可证。
