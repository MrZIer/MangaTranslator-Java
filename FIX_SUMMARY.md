# 🔧 问题修复说明

## 已修复的问题

### 1. 汇总文件夹识别问题
**问题**：汇总文件夹（`batch_xxx_collected_xxx`）无法被正确识别和显示

**修复**：
- 修改 `GalleryController.getBatchFolders()` 以正确识别包含 `_collected_` 的文件夹
- 对汇总文件夹统计图片数而非会话数
- 添加 `isCollected` 标记区分汇总文件夹和普通批量文件夹

### 2. 图片路径处理问题
**问题**：汇总文件夹的图片直接在根目录，而代码期望在 `translated` 子目录

**修复**：
- `getFolderImages()` 方法现在同时支持 `translated` 子目录和根目录
- `downloadImage()` 和 `viewImage()` 方法现在会先尝试 `translated` 子目录，如果不存在则尝试根目录
- `downloadFolder()` 方法也进行了相同的修复

### 3. 图片计数问题
**问题**：`createSessionFolderInfo()` 只检查 `translated` 子目录，忽略了汇总文件夹的根目录图片

**修复**：
- 现在会先检查 `translated` 子目录
- 如果不存在，则检查根目录（适用于汇总文件夹）

### 4. 前端显示问题
**问题**：前端无法正确显示汇总文件夹的信息

**修复**：
- 修改 `createFolderItem()` 以识别汇总文件夹
- 汇总文件夹显示图片数而非会话数
- 点击汇总文件夹直接显示图片而不是会话列表

## 测试步骤

### 1. 测试API（使用测试页面）
访问：http://localhost:8080/test-api.html

1. **测试批量文件夹API** - 应该显示所有批量文件夹和汇总文件夹
2. **测试单个文件夹API** - 应该显示所有单独的session文件夹
3. **测试图片列表API** - 输入文件夹路径测试

### 2. 测试图片展示页面
访问：http://localhost:8080/gallery.html

应该能看到：
- **左侧批量文件夹**：
  - `batch_20260131_171855` (显示 4 会话)
  - `batch_20260131_171855_collected_20260131_172041` (显示 4 图片)
  
- **左侧单个文件夹**：
  - `session_20260131_172613_560ab3ff` (显示 1 图片)

点击任意文件夹应该能看到图片列表

### 3. 测试主页面
访问：http://localhost:8080/index.html

- 历史记录应该正常显示（如果数据库中有记录）
- 翻译任务完成后应该显示"查看结果"按钮

## 文件夹结构说明

### 标准批量文件夹结构
```
batch_20260131_171855/
├── session_20260131_171855_64b4ce6f/
│   ├── original/
│   │   └── 016.jpg
│   └── translated/
│       └── 016_cn.jpg
├── session_20260131_171855_6e45e099/
│   └── ...
```
**特点**：包含多个session子文件夹，图片在 `translated` 子目录

### 汇总文件夹结构
```
batch_20260131_171855_collected_20260131_172041/
├── 016_cn.jpg
├── 017_cn.jpg
├── 018_cn.jpg
└── 019_cn.jpg
```
**特点**：图片直接在根目录，无子文件夹结构

### 单个会话文件夹结构
```
session_20260131_172613_560ab3ff/
├── original/
│   └── 015.jpg
└── translated/
    └── 015_cn.jpg
```
**特点**：单独的session，图片在 `translated` 子目录

## 当前存储状态

根据扫描结果，你的存储中有：

1. **batch_20260131_171855** (批量文件夹)
   - 包含 4 个 session
   - 每个 session 有 original 和 translated 子目录
   - 共 4 张翻译图片 (016~019_cn.jpg)

2. **batch_20260131_171855_collected_20260131_172041** (汇总文件夹)
   - 直接包含 4 张翻译图片 (016~019_cn.jpg)
   - 这是从上面批量文件夹汇总而来

3. **session_20260131_172613_560ab3ff** (单个会话)
   - 包含 original 和 translated 子目录
   - 1 张翻译图片 (015_cn.jpg)

## 预期结果

修复后，访问 http://localhost:8080/gallery.html 应该看到：

**批量文件夹区域**：
- ✅ batch_20260131_171855 (4 会话)
- ✅ batch_20260131_171855_collected_20260131_172041 (4 图片)

**单个文件夹区域**：
- ✅ session_20260131_172613_560ab3ff (1 图片)

点击任何一个文件夹都能正确显示图片！

## 如何验证修复

1. **重启应用**（如果还没重启的话）
2. 打开浏览器开发者工具（F12）
3. 访问 http://localhost:8080/test-api.html
4. 点击"测试批量文件夹API"按钮
5. 查看返回的JSON，应该包含所有文件夹及其正确的计数
6. 访问 http://localhost:8080/gallery.html
7. 验证左侧文件夹列表是否完整显示
8. 点击每个文件夹验证图片是否正确加载

## 如果仍有问题

请查看浏览器控制台（F12）的错误信息，并检查：
1. API返回的数据格式是否正确
2. 文件夹路径是否正确
3. 图片文件是否存在于预期位置
